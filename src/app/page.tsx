import Timer from '@/func/Timer';

export default function Home() {
    return (
        <main style={{ height: '100%', width: '100%', margin: '0px' }}>
            <Timer />
        </main>
    );
}
